<UserControl x:Class="UareU.Views.LoaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UareU.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <UserControl.Resources>
        <Storyboard x:Key="LocalSpin" RepeatBehavior="Forever">
            <DoubleAnimation
            Storyboard.TargetName="SpinnerEllipse"
            Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
            From="0" To="360"
            Duration="0:0:1"/>
        </Storyboard>
    </UserControl.Resources>
    <Grid x:Name="Root" Visibility="Collapsed">

        <!-- Blur background -->
        <Grid Background="#66000000">
            <Grid.Effect>
                <BlurEffect Radius="6"/>
            </Grid.Effect>
        </Grid>

        <!-- Card -->
        <Border x:Name="LoaderCard"
            Width="180"
            Height="180"
            CornerRadius="20"
            Background="#1E1E1E"
            Opacity="0"
            RenderTransformOrigin="0.5,0.5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            <Border.RenderTransform>
                <ScaleTransform x:Name="CardScale"
                            ScaleX="0.85"
                            ScaleY="0.85"/>
            </Border.RenderTransform>

            <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="20">

                <Grid Width="60" Height="60" Margin="0,0,0,10">
                    <!-- background ring -->
                    <Ellipse StrokeThickness="6" Stroke="#33FFFFFF"/>

                    <!-- spinning ring -->
                    <Ellipse x:Name="SpinnerEllipse"
             StrokeThickness="4"
             Stroke="White"
             StrokeDashArray="1 2"
             RenderTransformOrigin="0.5,0.5"/>
                </Grid>

                <TextBlock Text="Please wait"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"/>

                <TextBlock Text="Loading data..."
                       Foreground="#BFBFBF"
                       FontSize="12"
                       HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
